import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dharani.spt',
  appName: 'StudentProductivityTracker',
  webDir: 'dist'
};

export default config;
